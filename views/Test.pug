doctype html
html
  head
    style.
        .grid { 
            width: 90%;
            text-align: center;
            margin: 0 auto;
            display: flex;
            font-family: -apple-system,system-ui,"Segoe UI",Roboto,"Helvetica Neue",Ubuntu,sans-serif;
            font-weight: 500;
            justify-content: center;
            flex-wrap: wrap;
            border: 2px solid black;
        }

        .grid div {
            margin: 1em;
            flex: 0;
            width: 15%;
            min-width: 300px;
            height: 450px;
            position:relative;
        }

        .grid div div {
            width: 100%;
            position:absolute;
            bottom:0;
            margin: 0 auto;
            height: fit-content;
            text-align: center;
            background-color: rgb(10, 10, 10, .45);
            visibility: hidden;
        }

        .grid div:hover {
            transform: scale(1.2);
            z-index: 1;
        }

        .grid div:hover > div {
            visibility: visible;
        }

        #movieData{
            color: white;
        }

  body
    h1 Movie Homepage
    .grid
        for movie, i in movieNameArray
            div#movieData(style= "background: url(images/" + movieImageArray[i] + "); background-repeat: no-repeat; background-size: cover;") 
                div #{movie + " (" + releaseYearArray[i] + ")"}