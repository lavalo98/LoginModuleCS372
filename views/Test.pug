doctype html
style.
  table, th, td {
  border:1px solid black;
  }
  td{
      width: 280px;
      height: 418px;
      text-align: center;
      font-family: -apple-system,system-ui,"Segoe UI",Roboto,"Helvetica Neue",Ubuntu,sans-serif;
      display: table-cell;
      vertical-align: bottom;
      text-decoration: none;
      color: transparent;
      padding: 0px;
      border: 2px solid black;
  }
  h2{
      text-align: center;
  }
  td:hover {
    transform: scale(1.3);
    color: white;
    padding: 0;
    margin: 0;
  }
  td:hover > div{visibility: visible;}
  div{
      width: 100%;
      background-color: black;
      z-index: 5;
      visibility: hidden;
  }
  #movieData{
  }

h2 Movie Page Idea
table(style='width: auto; height: auto; margin: 0 auto; border-collapse: separate; border-spacing: 50px 50px;')
    tr 
        for movie, i in movieNameArray
            if i % 7 == 0 && i != 0
                tr
                td#movieData(style= "background: url(images/" + movieImageArray[i] + "); background-repeat: no-repeat; background-size: cover;")
                    div= movie
            else 
                td#movieData(style= "background: url(images/" + movieImageArray[i] + "); background-repeat: no-repeat; background-size: cover;")
                    div= movie